// JavaScript logic is primarily handled inline within the HTML files for page-specific GSAP and overlay menu functionality.
// Global utility functions can be added here.
document.addEventListener('DOMContentLoaded', () => {
    // Lucide icons are initialized inline on each page
});
